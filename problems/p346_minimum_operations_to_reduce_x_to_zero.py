DESCRIPTION = """Minimum Operations to Reduce X to Zero

You are given an integer array `nums` and an integer `x`. In one operation, you can
either remove the leftmost or the rightmost element from the array and subtract its
value from `x`.

Return the minimum number of operations to reduce `x` to exactly 0, or -1 if not
possible.

Example:
    Input: nums = [1, 1, 4, 2, 3], x = 5
    Output: 2
    Explanation: Remove the last two elements [2, 3] to reduce x to 0.

Example:
    Input: nums = [3, 2, 20, 1, 1, 3], x = 10
    Output: 5
"""


def solution(nums, x):
    return None
