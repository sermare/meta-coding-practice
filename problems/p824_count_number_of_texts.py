DESCRIPTION = """Count Number of Texts

Alice is texting Bob using her phone. The mapping of digits to letters is the
same as a standard phone keypad (2->abc, 3->def, ..., 7->pqrs, 8->tuv, 9->wxyz).

Given a string pressedKeys of digits, return the total number of possible text
messages Alice could have sent, modulo 10^9 + 7.

For example, pressing '2' could mean 'a', pressing '22' could mean 'aa' or 'b',
pressing '222' could mean 'aaa', 'ab', 'ba', or 'c'.

Example:
    Input: pressedKeys = "22233"
    Output: 8

"""


def solution(pressedKeys):
    return None
