DESCRIPTION = """Palindrome Partitioning

Given a string s, partition s such that every substring of the partition is a
palindrome. Return all possible palindrome partitions of s.

Example:
    Input: s = "aab"
    Output: [["a","a","b"],["aa","b"]]

    Input: s = "a"
    Output: [["a"]]
"""


def solution(s):
    return None
