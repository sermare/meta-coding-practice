DESCRIPTION = """Maximum Sum Circular Subarray

Given a circular integer array `nums` of length n, return the maximum possible sum
of a non-empty subarray of `nums`.

A circular array means the end of the array connects to the beginning. A subarray
may only include each element of the fixed buffer at most once.

Example:
    Input: nums = [1, -2, 3, -2]
    Output: 3
    Explanation: Subarray [3] has maximum sum 3.

Example:
    Input: nums = [5, -3, 5]
    Output: 10
    Explanation: Subarray [5, 5] (wrapping around) has maximum sum 10.
"""


def solution(nums):
    return None
