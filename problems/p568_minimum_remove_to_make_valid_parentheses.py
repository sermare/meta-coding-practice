DESCRIPTION = """Minimum Remove to Make Valid Parentheses

Given a string s of '(', ')' and lowercase English characters, remove the minimum number
of parentheses so that the resulting string is valid (every open parenthesis has a
matching close parenthesis and vice versa).

Return any valid string. A valid string is also considered valid if it is empty or
contains no parentheses.

Example:
    Input: s = "lee(t(c)o)de)"
    Output: "lee(t(c)o)de"

    Input: s = "a)b(c)d"
    Output: "ab(c)d"

    Input: s = "))(("
    Output: ""
"""


def solution(s):
    return None
