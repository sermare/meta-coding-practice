DESCRIPTION = """Flatten 2D Vector

Design an iterator to flatten a 2D vector. Implement the Vector2D class:
  - Vector2D(vec) initializes the object with the 2D vector vec.
  - next() returns the next element from the 2D vector and moves the pointer.
  - hasNext() returns True if there are still elements to traverse.

For this problem, implement a solution function that takes a 2D vector and a list
of operations, and returns the results of 'next' calls.

Example:
    Input: vec = [[1,2],[3],[4]]
    Output: [1, 2, 3, 4]
    Explanation: Calling next() repeatedly returns elements in order.
"""


def solution(vec):
    return None
