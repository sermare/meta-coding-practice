DESCRIPTION = """Sqrt(x)

Given a non-negative integer `x`, return the square root of x rounded down to the
nearest integer. The returned integer should be non-negative as well.

You must not use any built-in exponent function or operator (e.g., pow(x, 0.5) or
x ** 0.5).

Example:
    Input: x = 4
    Output: 2

Example:
    Input: x = 8
    Output: 2
    Explanation: The square root of 8 is 2.828..., rounded down to 2.
"""


def solution(x):
    return None
