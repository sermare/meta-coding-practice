DESCRIPTION = """Flatten Nested List Iterator

You are given a nested list of integers nestedList. Each element is either an
integer or a list whose elements may also be integers or other lists. Implement
an iterator to flatten it.

Implement the NestedIterator class:
- NestedIterator(nestedList) initializes the iterator with the nested list.
- next() returns the next integer in the nested list.
- hasNext() returns true if there are still some integers in the nested list.

For this problem, represent the nested list as a Python list where integers are
plain ints and nested lists are sublists.

Implement solution(nestedList) that returns a flat list of all integers.

Example:
    Input: [[1,1],2,[1,1]]
    Output: [1,1,2,1,1]
"""


def solution(nestedList):
    return None
