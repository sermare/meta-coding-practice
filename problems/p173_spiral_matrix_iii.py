DESCRIPTION = """Spiral Matrix III

You start at the cell (rStart, cStart) of a rows x cols grid facing east. The
grid is 0-indexed. You walk in a clockwise spiral shape to visit every position
in this grid. Whenever you move outside the grid boundary, you continue your
walk outside and may later return to the grid boundary.

Return an array of coordinates representing the positions of the grid cells in
the order you visit them.

Example:
    Input: rows = 1, cols = 4, rStart = 0, cStart = 0
    Output: [[0,0],[0,1],[0,2],[0,3]]

    Input: rows = 5, cols = 6, rStart = 1, cStart = 4
    Output: [[1,4],[1,5],[2,5],[2,4],[2,3],[1,3],[0,3],[0,4],[0,5],[3,5],...]
"""


def solution(rows, cols, rStart, cStart):
    return None
