DESCRIPTION = """Apply Discount to Prices

A sentence is a string of single-space separated words where each word can
contain digits, '$', and lowercase English letters. A word represents a price if
it starts with '$' followed by a non-negative number (with no leading zeros
unless it is "0").

Given a string sentence representing a sentence and an integer discount, return
a sentence where each price has been reduced by the given discount percentage,
formatted to exactly two decimal places.

Example:
    Input: sentence = "there are $1 $2 and 5$ candies in the shop", discount = 50
    Output: "there are $0.50 $1.00 and 5$ candies in the shop"
"""


def solution(sentence, discount):
    return None
