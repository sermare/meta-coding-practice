DESCRIPTION = """Kth Largest Element in a Stream

Design a class to find the kth largest element in a stream. Note that it is the kth
largest element in the sorted order, not the kth distinct element.

Implement the KthLargest class:
- KthLargest(int k, int[] nums) initializes the object with integer k and the stream
  of integers nums.
- int add(int val) appends the integer val to the stream and returns the element
  representing the kth largest element in the stream.

Example:
    Input: k = 3, nums = [4,5,8,2], adds = [3, 5, 10, 9, 4]
    Output: [4, 5, 5, 8, 8]
"""


def solution(k, nums, adds):
    """Simulate KthLargest operations. Return list of results from add()."""
    return None
