DESCRIPTION = """Map Sum Pairs

Design a map that allows you to:
- insert a key-value pair (key: string, val: int). If the key already exists,
  the original value will be overridden.
- sum(prefix) returns the sum of all the values whose key starts with the prefix.

Example:
    Input: ["MapSum","insert","sum","insert","sum"]
           [[], ["apple",3], ["ap"], ["app",2], ["ap"]]
    Output: [None, None, 3, None, 5]
"""


def solution(operations, arguments):
    return None
