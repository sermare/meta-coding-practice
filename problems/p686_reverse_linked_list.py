DESCRIPTION = """Reverse Linked List

Given the head of a singly linked list, reverse the list, and return the reversed
list as a regular Python list of values.

The input is given as a list of values representing the linked list.

Example:
    Input: head = [1, 2, 3, 4, 5]
    Output: [5, 4, 3, 2, 1]
"""


def solution(head):
    return None
