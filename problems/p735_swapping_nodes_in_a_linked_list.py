DESCRIPTION = """Swapping Nodes in a Linked List

You are given the head of a linked list, and an integer k.

Return the head of the linked list after swapping the values of the k-th node from the
beginning and the k-th node from the end (the list is 1-indexed).

The linked list is represented as a list of values.

Example:
    Input: head = [1, 2, 3, 4, 5], k = 2
    Output: [1, 4, 3, 2, 5]
"""


def solution(head, k):
    return None
