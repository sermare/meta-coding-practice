DESCRIPTION = """Merge Sorted Array

You are given two integer arrays `nums1` and `nums2`, sorted in non-decreasing order,
and two integers `m` and `n`, representing the number of elements in `nums1` and `nums2`
respectively.

Merge `nums2` into `nums1` as one sorted array. The final sorted array should be
returned (it is the modified nums1).

The first `m` elements of `nums1` denote the elements that should be merged. The last
`n` elements of `nums1` are set to 0 and should be ignored.

Example:
    Input: nums1 = [1,2,3,0,0,0], m = 3, nums2 = [2,5,6], n = 3
    Output: [1,2,2,3,5,6]
"""


def solution(nums1, m, nums2, n):
    return None
