DESCRIPTION = """Robot Room Cleaner

You are given a room represented as a grid where 1 means accessible and 0 means a
wall. A robot cleaner starts at an unknown position facing an unknown direction. The
robot can move forward, turn left, turn right, and clean the current cell.

For this problem, given the room grid and starting position, return the set of all
cells that should be cleaned (all accessible cells reachable from the start).

Example:
    Input: room = [[1,1,1],[1,0,1],[1,1,1]], row = 0, col = 0
    Output: [[0,0],[0,1],[0,2],[1,0],[1,2],[2,0],[2,1],[2,2]]
    Explanation: All cells with value 1 reachable from (0,0).
"""


def solution(room, row, col):
    return None
