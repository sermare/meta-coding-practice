DESCRIPTION = """Shortest Path in Binary Matrix

Given an n x n binary matrix `grid`, return the length of the shortest clear path in the
matrix. If there is no clear path, return -1.

A clear path starts at the top-left cell (0, 0) and ends at the bottom-right cell
(n-1, n-1) such that:
- All visited cells are 0.
- All adjacent cells (8-directionally) of the path are connected.

The length of a path is the number of visited cells.

Example:
    Input: grid = [[0,1],[1,0]]
    Output: 2

    Input: grid = [[0,0,0],[1,1,0],[1,1,0]]
    Output: 4

    Input: grid = [[1,0,0],[1,1,0],[1,1,0]]
    Output: -1
"""


def solution(grid):
    return None
