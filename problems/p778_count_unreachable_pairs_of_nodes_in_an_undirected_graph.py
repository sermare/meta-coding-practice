DESCRIPTION = """Count Unreachable Pairs of Nodes in an Undirected Graph

You are given an integer n. There is an undirected graph with n nodes, numbered
from 0 to n - 1. You are given a 2D integer array edges where edges[i] = [a, b]
means there is an edge between nodes a and b.

Return the number of pairs of different nodes that are unreachable from each
other.

Example:
    Input: n = 3, edges = [[0,1],[0,2],[1,2]]
    Output: 0
    Explanation: All nodes can reach each other so no pairs are unreachable.
"""


def solution(n, edges):
    return None
