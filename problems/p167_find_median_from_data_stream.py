DESCRIPTION = """Find Median from Data Stream

The median is the middle value in an ordered integer list. If the size of the
list is even, the median is the mean of the two middle values.

Implement the MedianFinder class:
- MedianFinder() initializes the MedianFinder object.
- addNum(num) adds the integer num to the data structure.
- findMedian() returns the median of all elements so far.

For this problem, implement solution(operations, args) that simulates the calls.

Example:
    Input: operations = ["addNum","addNum","findMedian","addNum","findMedian"]
           args = [[1],[2],[],[3],[]]
    Output: [None, None, 1.5, None, 2.0]
"""


def solution(operations, args):
    return None
