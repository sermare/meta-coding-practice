DESCRIPTION = """Gas Station

There are n gas stations along a circular route. You are given arrays gas and cost
where gas[i] is the amount of gas at station i and cost[i] is the cost to travel
from station i to station i+1. Return the starting station index if you can travel
around the circuit once clockwise, otherwise return -1. The solution is guaranteed
to be unique if it exists.

Example:
    Input: gas = [1,2,3,4,5], cost = [3,4,5,1,2]
    Output: 3
"""


def solution(gas, cost):
    return None
