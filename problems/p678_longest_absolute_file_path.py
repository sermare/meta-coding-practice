DESCRIPTION = """Longest Absolute File Path

Given a string `input` representing the file system in an explained format where
'\\n' and '\\t' represent newlines and tabs, return the length of the longest absolute
path to a file. If there is no file in the system, return 0.

A file is identified by having a '.' in its name.

Example:
    Input: input = "dir\\n\\tsubdir1\\n\\tsubdir2\\n\\t\\tfile.ext"
    Output: 20
    Explanation: The path is "dir/subdir2/file.ext" with length 20.
"""


def solution(input):
    return None
