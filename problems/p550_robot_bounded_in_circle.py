DESCRIPTION = """Robot Bounded In Circle

On an infinite plane, a robot starts at (0, 0) facing north. The robot receives
instructions as a string: "G" (go forward 1 unit), "L" (turn 90 degrees left),
"R" (turn 90 degrees right). Return True if and only if there exists a circle
such that the robot never leaves the circle after executing the instructions
repeatedly.

Example:
    Input: instructions = "GGLLGG"
    Output: True
    Explanation: The robot returns to (0,0) after one cycle.
"""


def solution(instructions):
    return None
