DESCRIPTION = """Find Nearest Point That Has Same X or Y Coordinate

You are given two integers x and y, which represent your current location on a
Cartesian grid: (x, y). You are also given an array points where each points[i] =
[ai, bi] represents that a point exists at (ai, bi).

A point is valid if it shares the same x-coordinate or the same y-coordinate as your
location.

Return the index (0-indexed) of the valid point with the smallest Manhattan distance
from your current location. If there are multiple, return the smallest index. If there
are no valid points, return -1.

Example:
    Input: x = 3, y = 4, points = [[1,2],[3,1],[2,4],[2,3],[4,4]]
    Output: 2
    Explanation: Points [3,1] and [2,4] and [4,4] are valid. [2,4] has smallest distance.
"""


def solution(x, y, points):
    return None
