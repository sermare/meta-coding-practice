DESCRIPTION = """Complex Number Multiplication

A complex number can be represented as a string on the form "real+imaginaryi" where:
- real is the integer part and can be in the range [-100, 100].
- imaginary is the imaginary part and can be in the range [-100, 100].
- i^2 == -1.

Given two complex numbers num1 and num2 as strings, return a string of the complex
number that represents their multiplication.

Example:
    Input: num1 = "1+1i", num2 = "1+1i"
    Output: "0+2i"
    Explanation: (1+i) * (1+i) = 1 + i^2 + 2*i = 2i.

    Input: num1 = "1+-1i", num2 = "1+-1i"
    Output: "0+-2i"
"""


def solution(num1, num2):
    return None
