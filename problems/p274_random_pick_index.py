DESCRIPTION = """Random Pick Index

Given an integer array nums with possible duplicates, randomly output the index
of a given target number. You can assume that the given target number must exist
in the array.

Implement the solution function that takes nums and target and returns a valid
index where nums[index] == target. Each valid index should have equal probability
of being returned.

This problem uses reservoir sampling.

Example:
    Input: nums = [1,2,3,3,3], target = 3
    Output: 2 or 3 or 4 (randomly, with equal probability)
"""


def solution(nums, target):
    return None
