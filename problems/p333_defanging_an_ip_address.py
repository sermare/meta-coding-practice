DESCRIPTION = """Defanging an IP Address

Given a valid (IPv4) IP address `address`, return a defanged version of that IP
address. A defanged IP address replaces every period "." with "[.]".

Example:
    Input: address = "1.1.1.1"
    Output: "1[.]1[.]1[.]1"

Example:
    Input: address = "255.100.50.0"
    Output: "255[.]100[.]50[.]0"
"""


def solution(address):
    return None
