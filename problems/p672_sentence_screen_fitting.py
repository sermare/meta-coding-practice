DESCRIPTION = """Sentence Screen Fitting

Given a rows x cols screen and a sentence represented as a list of non-empty words,
return the number of times the given sentence can be fitted on the screen.

Words cannot be split across lines. A word's order follows the order in the sentence.
The same word in the sentence may be used more than once. Each word is separated by a
single space. No leading or trailing spaces.

Example:
    Input: sentence = ["hello","world"], rows = 2, cols = 8
    Output: 1
    Explanation:
        hello---
        world---
        The character '-' signifies an empty space on the screen.
"""


def solution(sentence, rows, cols):
    return None
