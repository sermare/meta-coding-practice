DESCRIPTION = """Design a Number Container System

Design a number container system that can do the following:
- Insert or replace a number at the given index in the system.
- Return the smallest index for the given number in the system.

Implement the NumberContainers class:
- NumberContainers() initializes the number container system.
- change(index, number) fills the container at index with the number.
- find(number) returns the smallest index for the given number, or -1.

Example:
    Input:
        nc = NumberContainers()
        nc.find(10)       -> -1
        nc.change(2, 10)
        nc.change(1, 10)
        nc.find(10)       -> 1
        nc.change(1, 20)
        nc.find(10)       -> 2
"""


def solution(operations, arguments):
    return None
