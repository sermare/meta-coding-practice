DESCRIPTION = """Sum of Digits of String After Convert

You are given a string s consisting of lowercase English letters, and an integer
k. First, convert s into an integer by replacing each letter with its position
in the alphabet (a=1, b=2, ..., z=26) and concatenating all the integers.
Then, transform the integer by replacing it with the sum of its digits. Repeat
the transform operation k times in total.

Return the resulting integer after performing the operations.

Example:
    Input: s = "iiii", k = 1
    Output: 36
    Explanation: "iiii" -> "9999" -> 9+9+9+9 = 36.
"""


def solution(s, k):
    return None
