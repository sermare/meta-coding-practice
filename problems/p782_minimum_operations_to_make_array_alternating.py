DESCRIPTION = """Minimum Operations to Make Array Alternating

You are given a 0-indexed array nums consisting of n positive integers. The
array nums is called alternating if nums[i-2] == nums[i] for every 2 <= i <= n-1
and nums[i-1] != nums[i] for every 1 <= i <= n-1. In other words, even-indexed
and odd-indexed elements alternate values.

In one operation, you can choose an index i and change nums[i] to any positive
integer. Return the minimum number of operations needed to make the array
alternating.

Example:
    Input: nums = [3,1,3,2,4,3]
    Output: 3
    Explanation: Change to [3,1,3,1,3,1]. Three changes needed.
"""


def solution(nums):
    return None
