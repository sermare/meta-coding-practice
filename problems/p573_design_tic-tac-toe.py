DESCRIPTION = """Design Tic-Tac-Toe

Assume the following rules are for the tic-tac-toe game on an n x n board between two
players:
- A move is guaranteed to be valid and is placed on an empty block.
- Once a winning condition is reached, no more moves are allowed.
- A player who succeeds in placing n of their marks in a horizontal, vertical, or
  diagonal row wins the game.

Implement the TicTacToe class:
- TicTacToe(n) initializes the object with the size of the board n.
- move(row, col, player) indicates that the player with id player plays at cell
  (row, col). The move is guaranteed to be valid. Return the player id if the player
  wins, otherwise return 0.

For this problem, given n and a list of moves, return the list of results.

Example:
    Input: n = 3, moves = [[0,0,1],[0,2,2],[2,2,1],[1,1,2],[2,0,1],[1,0,2],[2,1,1]]
    Output: [0,0,0,0,0,0,1]
"""


def solution(n, moves):
    return None
