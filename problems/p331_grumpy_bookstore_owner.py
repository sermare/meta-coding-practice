DESCRIPTION = """Grumpy Bookstore Owner

There is a bookstore owner that has a store open for n minutes. Every minute, some
number of customers enter the store given by `customers[i]`.

The owner is grumpy at minute i if `grumpy[i] == 1`, otherwise not grumpy. When the
owner is grumpy, customers at that minute are not satisfied.

The owner can use a secret technique to not be grumpy for `minutes` consecutive
minutes, but can only use it once.

Return the maximum number of satisfied customers.

Example:
    Input: customers = [1,0,1,2,1,1,7,5], grumpy = [0,1,0,1,0,1,0,1], minutes = 3
    Output: 16
"""


def solution(customers, grumpy, minutes):
    return None
