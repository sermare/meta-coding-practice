DESCRIPTION = """Unique Word Abbreviation

The abbreviation of a word is a concatenation of its first letter, the number of
characters between the first and last letter, and its last letter. If the word has only
two characters, then it is an abbreviation of itself.

Implement the ValidWordAbbr class:
- ValidWordAbbr(dictionary) initializes the object with a dictionary of words.
- isUnique(word) returns True if either: the abbreviation of word is not in the
  dictionary, or all words in the dictionary with that abbreviation are the same as word.

For this problem, given a dictionary and a list of query words, return a list of booleans.

Example:
    Input: dictionary = ["deer","door","cake","card"],
           queries = ["dear","cart","cane","make"]
    Output: [False, True, False, True]
"""


def solution(dictionary, queries):
    return None
