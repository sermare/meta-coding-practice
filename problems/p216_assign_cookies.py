DESCRIPTION = """Assign Cookies

You are an awesome parent and want to give your children some cookies. Each child i
has a greed factor g[i], which is the minimum size of a cookie that the child will be
content with; and each cookie j has a size s[j].

If s[j] >= g[i], we can assign cookie j to child i, and the child becomes content.
Maximize the number of content children and output the maximum number.

Example:
    Input: g = [1, 2, 3], s = [1, 1]
    Output: 1

Example:
    Input: g = [1, 2], s = [1, 2, 3]
    Output: 2
"""


def solution(g, s):
    return None
