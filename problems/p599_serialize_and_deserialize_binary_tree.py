DESCRIPTION = """Serialize and Deserialize Binary Tree

Design an algorithm to serialize and deserialize a binary tree. Serialization is the
process of converting a data structure into a sequence of bits so that it can be stored
or transmitted and reconstructed later.

For this problem, given a binary tree as a level-order list, serialize it to a string and
then deserialize it back. Return the level-order list of the reconstructed tree. The
result should be identical to the input.

Example:
    Input: root = [1,2,3,None,None,4,5]
    Output: [1,2,3,None,None,4,5]

    Input: root = []
    Output: []

    Input: root = [1]
    Output: [1]
"""


def solution(root):
    return None
