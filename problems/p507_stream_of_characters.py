DESCRIPTION = """Stream of Characters

Design an algorithm that accepts a stream of characters and checks if a suffix
of the characters so far is a string of a given list of strings `words`.

Implement the StreamChecker class:
- StreamChecker(words) initializes the object with the given words.
- query(letter) returns True if any suffix of the stream of characters queried
  so far is in words, else False.

Example:
    Input: ["StreamChecker","query","query","query","query","query","query"]
           [[["cd","f","kl"]],["a"],["b"],["c"],["d"],["e"],["f"]]
    Output: [None, False, False, False, True, False, True]
"""


def solution(operations, arguments):
    return None
