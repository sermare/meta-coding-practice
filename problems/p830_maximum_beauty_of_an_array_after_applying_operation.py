DESCRIPTION = """Maximum Beauty of an Array After Applying Operation

You are given a 0-indexed array nums and a non-negative integer k. In one
operation, you can replace nums[i] with any integer in the range
[nums[i] - k, nums[i] + k].

The beauty of the array is the length of the longest subsequence consisting
of equal elements. You can apply the operation to each index once.

Return the maximum possible beauty of the array nums after applying the
operation to each index.

Example:
    Input: nums = [4,6,1,2], k = 2
    Output: 3
    Explanation: Apply operation to make [4,4,3,4], beauty = 3.

"""


def solution(nums, k):
    return None
