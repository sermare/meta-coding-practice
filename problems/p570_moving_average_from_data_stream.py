DESCRIPTION = """Moving Average from Data Stream

Given a stream of integers and a window size, calculate the moving average of all
integers in the sliding window.

Implement the MovingAverage class:
- MovingAverage(size) initializes the object with the size of the window.
- next(val) returns the moving average of the last `size` values of the stream.

For this problem, given the window size and a list of values, return the list of moving
averages after each call to next().

Example:
    Input: size = 3, vals = [1, 10, 3, 5]
    Output: [1.0, 5.5, 4.66667, 6.0]
"""


def solution(size, vals):
    return None
