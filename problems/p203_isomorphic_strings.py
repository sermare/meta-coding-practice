DESCRIPTION = """Isomorphic Strings

Given two strings `s` and `t`, determine if they are isomorphic.

Two strings s and t are isomorphic if the characters in s can be replaced to get t,
with a one-to-one mapping. No two characters may map to the same character, but a
character may map to itself.

Example:
    Input: s = "egg", t = "add"
    Output: True

Example:
    Input: s = "foo", t = "bar"
    Output: False
"""


def solution(s, t):
    return None
