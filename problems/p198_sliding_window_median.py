DESCRIPTION = """Sliding Window Median

The median is the middle value in an ordered integer list. If the size is even,
the median is the mean of the two middle values.

Given an array nums and an integer k, there is a sliding window of size k which
moves from the very left to the very right. Each time the window moves right by
one position, return the median of the window.

Example:
    Input: nums = [1,3,-1,-3,5,3,6,7], k = 3
    Output: [1.0,-1.0,-1.0,3.0,5.0,6.0]
"""


def solution(nums, k):
    return None
