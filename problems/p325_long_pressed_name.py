DESCRIPTION = """Long Pressed Name

Your friend is typing his `name` into a keyboard. Sometimes, a key might get long
pressed, and the character will be typed 1 or more times.

You examine the `typed` characters of the keyboard. Return True if it is possible
that it was your friends name, with some characters being long pressed.

Example:
    Input: name = "alex", typed = "aaleex"
    Output: True

Example:
    Input: name = "saeed", typed = "ssaaedd"
    Output: False
"""


def solution(name, typed):
    return None
