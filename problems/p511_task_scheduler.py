DESCRIPTION = """Task Scheduler

Given a characters array `tasks` representing CPU tasks labeled A-Z and a non-negative
integer `n` representing the cooldown interval between two same tasks, return the least
number of units of time the CPU will take to finish all the given tasks. Each unit of
time the CPU can either complete a task or be idle.

Example:
    Input: tasks = ["A","A","A","B","B","B"], n = 2
    Output: 8
    Explanation: A -> B -> idle -> A -> B -> idle -> A -> B
"""


def solution(tasks, n):
    return None
