DESCRIPTION = """Implement Trie (Prefix Tree)

A trie (pronounced as "try") or prefix tree is a tree data structure used to
efficiently store and retrieve keys in a dataset of strings. Implement the Trie
class with the following methods:

- Trie() initializes the trie object.
- insert(word) inserts the string `word` into the trie.
- search(word) returns True if the string `word` is in the trie, else False.
- startsWith(prefix) returns True if there is a previously inserted string that
  has the prefix `prefix`, else False.

Example:
    Input: ["Trie", "insert", "search", "search", "startsWith", "insert", "search"]
           [[], ["apple"], ["apple"], ["app"], ["app"], ["app"], ["app"]]
    Output: [None, None, True, False, True, None, True]
"""


def solution(operations, arguments):
    return None
