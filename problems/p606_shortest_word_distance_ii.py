DESCRIPTION = """Shortest Word Distance II

Design a data structure that will be initialized with a string array, and
then should answer queries of the shortest distance between two different
strings from the array.

Implement the WordDistance class:
- WordDistance(wordsDict): Initializes with the string array wordsDict.
- shortest(word1, word2): Returns the shortest distance between word1 and
  word2 in the array.

Example:
    Input: wordsDict = ["practice","makes","perfect","coding","makes"]
           shortest("coding","practice") -> 3
           shortest("makes","coding") -> 1
"""


def solution(wordsDict, queries):
    return None
