DESCRIPTION = """Plus One

You are given a large integer represented as an integer array `digits`, where each
digits[i] is the ith digit. The digits are ordered from most significant to least.
Increment the large integer by one and return the resulting array.

Example:
    Input: digits = [1,2,3]
    Output: [1,2,4]
"""


def solution(digits):
    return None
