DESCRIPTION = """Maximum Profit in Job Scheduling

We have n jobs where every job is scheduled to be done from startTime[i] to
endTime[i], obtaining a profit of profit[i]. Return the maximum profit you can
take such that there are no two jobs that overlap (a job that ends at time X
can start at time X).

Example:
    Input: startTime = [1,2,3,3], endTime = [3,4,5,6], profit = [50,10,40,70]
    Output: 120
"""


def solution(startTime, endTime, profit):
    return None
