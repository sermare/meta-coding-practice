DESCRIPTION = """Count Number of Nice Subarrays

Given an array of integers `nums` and an integer `k`, a continuous subarray is called
nice if there are `k` odd numbers in it.

Return the number of nice sub-arrays.

Example:
    Input: nums = [1, 1, 2, 1, 1], k = 3
    Output: 2
    Explanation: Subarrays [1,1,2,1] and [1,2,1,1] contain exactly 3 odd numbers.

Example:
    Input: nums = [2, 2, 2, 1, 2, 2, 1, 2, 2, 2], k = 2
    Output: 16
"""


def solution(nums, k):
    return None
