DESCRIPTION = """Peeking Iterator

Design an iterator that supports the peek operation on an existing iterator in
addition to the hasNext and the next operations.

Implement the PeekingIterator class:
- PeekingIterator(Iterator nums) Initializes the object with the given integer iterator.
- int next() Returns the next element in the array and moves the pointer to the next element.
- boolean hasNext() Returns true if there are still elements in the array.
- int peek() Returns the next element in the array without moving the pointer.

Example:
    Input: nums = [1, 2, 3]
           peekingIterator.next()    -> 1
           peekingIterator.peek()    -> 2
           peekingIterator.next()    -> 2
    Output: [1, 2, 2]
"""


def solution(nums, operations):
    return None
