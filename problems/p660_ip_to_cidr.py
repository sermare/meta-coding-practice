DESCRIPTION = """IP to CIDR

Given a start IP address `ip` and an integer `n`, return the minimum list of CIDR
blocks that covers all IPs from the starting IP to the next n IPs (inclusive of the
start).

A CIDR block is a string like "255.0.0.7/32" representing an IP range.

Example:
    Input: ip = "255.0.0.7", n = 10
    Output: ["255.0.0.7/32","255.0.0.8/29","255.0.0.16/32"]
"""


def solution(ip, n):
    return None
