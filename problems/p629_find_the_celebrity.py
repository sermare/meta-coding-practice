DESCRIPTION = """Find the Celebrity

Suppose you are at a party with n people labeled from 0 to n - 1, and among
them there may exist one celebrity. The definition of a celebrity is that all
the other n - 1 people know the celebrity, but the celebrity does not know
any of them.

You are given a helper function knows(a, b) which tells you whether a knows
b. Implement a function to find the celebrity. If there is no celebrity,
return -1. You should minimize the number of calls to knows.

Example:
    Input: graph = [[1,1,0],[0,1,0],[1,1,1]]
    Output: 1
    Explanation: Person 1 is the celebrity.

Example:
    Input: graph = [[1,0,1],[1,1,0],[0,1,1]]
    Output: -1
"""


def solution(knows, n):
    return None
