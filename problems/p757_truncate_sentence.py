DESCRIPTION = """Truncate Sentence

A sentence is a list of words that are separated by a single space with no
leading or trailing spaces. Each of the words consists of only uppercase and
lowercase English letters (no punctuation).

Given a sentence s and an integer k, return the sentence truncated such that it
contains only the first k words.

Example:
    Input: s = "Hello how are you Contestant", k = 4
    Output: "Hello how are you"
"""


def solution(s, k):
    return None
