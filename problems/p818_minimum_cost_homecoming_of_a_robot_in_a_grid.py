DESCRIPTION = """Minimum Cost Homecoming of a Robot in a Grid

There is an m x n grid, where (0, 0) is the top-left cell and (m - 1, n - 1)
is the bottom-right cell. You are given an integer array startPos where
startPos = [startrow, startcol] indicates the robot's initial position, and
an integer array homePos where homePos = [homerow, homecol] indicates the
robot's home position.

The robot needs to go to its home. Moving to an adjacent cell costs
rowCosts[r] or colCosts[c] depending on which row/column you enter.

Return the minimum total cost for the robot to return home.

Example:
    Input: startPos = [1,0], homePos = [2,3], rowCosts = [5,4,3], colCosts = [8,2,6,7]
    Output: 18

"""


def solution(startPos, homePos, rowCosts, colCosts):
    return None
