DESCRIPTION = """Shortest Bridge

You are given an n x n binary matrix `grid` where 1 represents land and 0 represents
water.

An island is a 4-directionally connected group of 1's. There are exactly two islands
in grid.

Return the smallest number of 0's you must flip to connect the two islands.

Example:
    Input: grid = [[0,1],[1,0]]
    Output: 1

Example:
    Input: grid = [[0,1,0],[0,0,0],[0,0,1]]
    Output: 2
"""


def solution(grid):
    return None
