DESCRIPTION = """Max Stack

Design a max stack data structure that supports the stack operations and
supports finding the stack's maximum element.

Implement the MaxStack class:
- MaxStack(): Initializes the stack object.
- push(x): Pushes element x onto the stack.
- pop(): Removes the element on top of the stack and returns it.
- top(): Gets the element on the top of the stack without removing it.
- peekMax(): Retrieves the maximum element in the stack without removing it.
- popMax(): Retrieves the maximum element in the stack and removes it.

Example:
    Input: ["MaxStack","push","push","push","top","popMax","top","peekMax","pop","top"]
           [[],[5],[1],[5],[],[],[],[],[],[]]
    Output: [null,null,null,null,5,5,1,5,1,5]
"""


def solution(operations, arguments):
    return None
