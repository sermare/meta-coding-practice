DESCRIPTION = """Number of Atoms

Given a string `formula` representing a chemical formula, return the count of each atom.

The atomic element always starts with an uppercase character, then zero or more lowercase
letters. If the count is 1, it is omitted.

If there is a group (within parentheses), the count after the group multiplies all the
counts inside.

Return the result as a string of elements sorted lexicographically with their counts.

Example:
    Input: formula = "H2O"
    Output: "H2O"

Example:
    Input: formula = "Mg(OH)2"
    Output: "H2MgO2"

Example:
    Input: formula = "K4(ON(SO3)2)2"
    Output: "K4N2O14S4"
"""


def solution(formula):
    return None
