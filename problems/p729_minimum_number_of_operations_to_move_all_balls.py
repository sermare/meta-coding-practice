DESCRIPTION = """Minimum Number of Operations to Move All Balls

You have n boxes. You are given a binary string boxes of length n, where boxes[i] is
'0' if the i-th box is empty, and '1' if it contains one ball.

In one operation, you can move one ball from a box to an adjacent box. Box i is adjacent
to box i-1 and box i+1.

Return an array answer of size n, where answer[i] is the minimum number of operations
needed to move all the balls to the i-th box.

Example:
    Input: boxes = "110"
    Output: [1, 1, 3]
"""


def solution(boxes):
    return None
