DESCRIPTION = """Insert Delete GetRandom O(1)

Implement the RandomizedSet class:
- RandomizedSet() initializes the object.
- insert(val) inserts val if not present, returns True, else False.
- remove(val) removes val if present, returns True, else False.
- getRandom() returns a random element from the set. Each element must have the same
  probability of being returned.

Each function must work in average O(1) time complexity.

The solution function receives a list of operations and a list of arguments, and
returns a list of results.

Example:
    Input: ops = ["insert", "remove", "insert", "getRandom", "remove", "insert", "getRandom"],
           args = [[1], [2], [2], [], [1], [2], []]
    Output: [True, False, True, 2, True, False, 2]
"""


def solution(ops, args):
    return None
