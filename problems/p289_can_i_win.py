DESCRIPTION = """Can I Win

In the "100 game" two players take turns adding, to a running total, any integer
from 1 to 10. The player who first causes the running total to reach or exceed 100
wins.

What if we change the game so that players cannot re-use integers?

Given two integers maxChoosableInteger and desiredTotal, return true if the first
player to move can force a win, assuming both players play optimally.

Constraints:
- 1 <= maxChoosableInteger <= 20
- 0 <= desiredTotal <= 300

Example:
    Input: maxChoosableInteger = 10, desiredTotal = 11
    Output: False
"""


def solution(maxChoosableInteger, desiredTotal):
    return None
