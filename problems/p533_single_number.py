DESCRIPTION = """Single Number

Given a non-empty array of integers `nums`, every element appears twice except for
one. Find that single one. You must implement a solution with O(n) time and O(1)
extra space.

Example:
    Input: nums = [2,2,1]
    Output: 1
"""


def solution(nums):
    return None
