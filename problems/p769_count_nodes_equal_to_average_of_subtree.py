DESCRIPTION = """Count Nodes Equal to Average of Subtree

Given the root of a binary tree, return the number of nodes where the value of
the node is equal to the average of the values in its subtree.

The average of n elements is the sum of the n elements divided by n and rounded
down to the nearest integer. The subtree of a node includes the node itself and
all of its descendants.

For this problem, the tree is given as a list in level-order (BFS) format, where
None represents a missing node.

Example:
    Input: root = [4,8,5,0,1,None,6]
    Output: 5
"""


def solution(root):
    return None
