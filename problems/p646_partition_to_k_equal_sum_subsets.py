DESCRIPTION = """Partition to K Equal Sum Subsets

Given an integer array `nums` and an integer `k`, return True if it is
possible to divide this array into k non-empty subsets whose sums are all
equal.

Example:
    Input: nums = [4,3,2,3,5,2,1], k = 4
    Output: True
    Explanation: Possible to divide into 4 subsets (5), (1,4), (2,3), (2,3)
                 with equal sum.

Example:
    Input: nums = [1,2,3,4], k = 3
    Output: False
"""


def solution(nums, k):
    return None
