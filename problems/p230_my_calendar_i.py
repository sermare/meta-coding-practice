DESCRIPTION = """My Calendar I

You are implementing a program to use as your calendar. You can add a new event if
adding the event will not cause a double booking.

A double booking happens when two events have some non-empty intersection (i.e., some
moment is common to both events).

Implement the MyCalendar class:
- book(start, end): Returns True if the event can be added without causing a double
  booking. The event is [start, end) (half-open interval).

Example:
    Input: bookings = [[10,20],[15,25],[20,30]]
    Output: [True, False, True]
"""


def solution(bookings):
    """Simulate MyCalendar bookings. Return list of results."""
    return None
