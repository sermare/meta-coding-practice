DESCRIPTION = """Text Justification

Given an array of strings `words` and a width `maxWidth`, format the text
such that each line has exactly maxWidth characters and is fully (left and
right) justified.

Pack as many words as you can in each line. Pad extra spaces when necessary
so that each line has exactly maxWidth characters. Extra spaces between words
should be distributed as evenly as possible. If the number of spaces does not
divide evenly, the empty slots on the left will be assigned more spaces than
the right. The last line should be left-justified with no extra space between
words.

Example:
    Input: words = ["This","is","an","example","of","text","justification."],
           maxWidth = 16
    Output: ["This    is    an", "example  of text", "justification.  "]
"""


def solution(words, maxWidth):
    return None
