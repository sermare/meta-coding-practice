DESCRIPTION = """Implement Queue using Stacks

Implement a first in first out (FIFO) queue using only two stacks. The implemented queue
should support all the functions of a normal queue (push, peek, pop, and empty).

For this problem, given a list of operations and their arguments, return the results.

Example:
    Input: ops = ["push","push","peek","pop","empty"],
           args = [[1],[2],[],[],[]]
    Output: [None, None, 1, 1, False]
"""


def solution(ops, args):
    return None
