DESCRIPTION = """Number of Atoms

Given a string `formula` representing a chemical formula, return the count of each atom.

The atomic element starts with an uppercase character, then zero or more lowercase
letters. A count (optionally) follows, representing the number of that element. If the
count is missing, it is 1. Parentheses group sub-formulas and a count after the closing
parenthesis multiplies all counts inside.

Return the result as a string with elements sorted alphabetically, each followed by its
count (if the count is more than 1).

Example:
    Input: formula = "H2O"
    Output: "H2O"

    Input: formula = "Mg(OH)2"
    Output: "H2MgO2"

    Input: formula = "K4(ON(SO3)2)2"
    Output: "K4N2O14S4"
"""


def solution(formula):
    return None
