DESCRIPTION = """Two Sum II - Input Array Is Sorted

Given a 1-indexed array of integers `numbers` that is already sorted in non-decreasing
order, find two numbers such that they add up to a specific `target` number.

Return the indices of the two numbers (1-indexed) as a list [index1, index2] where
1 <= index1 < index2 <= numbers.length.

You may not use the same element twice. There is exactly one solution.

Example:
    Input: numbers = [2, 7, 11, 15], target = 9
    Output: [1, 2]
    Explanation: 2 + 7 = 9, so index1 = 1, index2 = 2.
"""


def solution(numbers, target):
    return None
