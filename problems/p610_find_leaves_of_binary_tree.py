DESCRIPTION = """Find Leaves of Binary Tree

Given the root of a binary tree, collect a tree's nodes as if you were
doing this: collect all the leaf nodes, remove all the leaf nodes, repeat
until the tree is empty.

Return a list of lists of leaf values at each step.

Example:
    Input: root = [1,2,3,4,5]
    Output: [[4,5,3],[2],[1]]
    Explanation: First collect leaves [4,5,3], then [2], then [1].

Example:
    Input: root = [1]
    Output: [[1]]
"""


def solution(root):
    return None
