DESCRIPTION = """Map of Highest Peak

You are given an integer matrix isWater of size m x n that represents a map of land
and water cells.

- If isWater[i][j] == 0, cell (i, j) is a land cell.
- If isWater[i][j] == 1, cell (i, j) is a water cell.

You must assign each cell a height in a way that:
- The height of each water cell is 0.
- Any two adjacent cells must have an absolute height difference of at most 1.

Find an assignment of heights such that the maximum height in the matrix is maximized.

Return an integer matrix height of size m x n where height[i][j] is the height of
cell (i, j).

Example:
    Input: isWater = [[0,1],[0,0]]
    Output: [[1,0],[2,1]]
"""


def solution(isWater):
    return None
