DESCRIPTION = """Count Hills and Valleys in an Array

You are given a 0-indexed integer array nums. An index i is part of a hill if
the closest non-equal neighbors of i are smaller than nums[i]. Similarly, an
index i is part of a valley if the closest non-equal neighbors of i are larger
than nums[i].

Return the number of hills and valleys in nums. Adjacent indices with the same
value are considered as part of the same hill or valley.

Example:
    Input: nums = [2,4,1,1,6,5]
    Output: 3
    Explanation: Index 1 is hill, indices 2-3 are valley, index 4 is hill.
"""


def solution(nums):
    return None
