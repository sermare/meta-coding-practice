DESCRIPTION = """Range Sum Query - Immutable

Given an integer array `nums`, handle multiple queries of the following type:

Calculate the sum of the elements of nums between indices left and right inclusive
where left <= right.

Implement the NumArray class:
- NumArray(nums): Initializes the object with the integer array nums.
- sumRange(left, right): Returns the sum of the elements between indices left and
  right inclusive.

Example:
    Input: nums = [-2, 0, 3, -5, 2, -1], queries = [[0,2],[2,5],[0,5]]
    Output: [1, -1, -3]
"""


def solution(nums, queries):
    """Return a list of sumRange results for each query [left, right]."""
    return None
