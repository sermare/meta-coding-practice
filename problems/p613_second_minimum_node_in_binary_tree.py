DESCRIPTION = """Second Minimum Node In a Binary Tree

Given a non-empty special binary tree consisting of nodes with the
non-negative value, where each node in this tree has exactly two or zero
sub-nodes. If the node has two sub-nodes, then this node's value is the
smaller value among its two sub-nodes.

Given such a binary tree, output the second minimum value in the set made
of all the nodes' values. If no such second minimum value exists, output -1.

Example:
    Input: root = [2,2,5,null,null,5,7]
    Output: 5

Example:
    Input: root = [2,2,2]
    Output: -1
"""


def solution(root):
    return None
