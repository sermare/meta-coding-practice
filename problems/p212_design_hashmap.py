DESCRIPTION = """Design HashMap

Design a HashMap without using any built-in hash table libraries.

Implement the MyHashMap class:
- put(key, value): inserts a (key, value) pair. If key already exists, update the value.
- get(key): returns the value mapped to key, or -1 if no mapping exists.
- remove(key): removes the key and its value if it exists.

Example:
    Input: ops = ["put","put","get","get","put","get","remove","get"]
           vals = [[1,1],[2,2],[1],[3],[2,1],[2],[2],[2]]
    Output: [None, None, 1, -1, None, 1, None, -1]
"""


def solution(operations, values):
    """Simulate MyHashMap operations. Return list of results."""
    return None
