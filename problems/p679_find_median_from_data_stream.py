DESCRIPTION = """Find Median from Data Stream

The median is the middle value in an ordered integer list. If the size is even, the
median is the mean of the two middle values.

Implement a solution that takes a list of operations: "add X" adds X to the data
structure, "find" returns the current median. Return a list of medians for each
"find" operation.

Example:
    Input: operations = ["add 1", "add 2", "find", "add 3", "find"]
    Output: [1.5, 2.0]
    Explanation: After adding 1,2 median is 1.5. After adding 3, median is 2.0.
"""


def solution(operations):
    return None
