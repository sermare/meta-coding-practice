DESCRIPTION = """Valid Parenthesis String

Given a string s containing only three types of characters: '(', ')' and '*',
return True if s is valid. '*' could be treated as '(', ')' or an empty string.

Example:
    Input: s = "(*))"
    Output: True
"""


def solution(s):
    return None
