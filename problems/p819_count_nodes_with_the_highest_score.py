DESCRIPTION = """Count Nodes With the Highest Score

There is a binary tree rooted at 0 consisting of n nodes. The nodes are labeled
from 0 to n - 1. You are given a 0-indexed integer array parents representing
the tree, where parents[i] is the parent of node i. Since node 0 is the root,
parents[0] == -1.

Each node has a score: if we remove that node, the tree splits into connected
components. The score of a node is the product of the sizes of those components.

Return the number of nodes that have the highest score.

Example:
    Input: parents = [-1, 2, 0, 2, 0]
    Output: 3

"""


def solution(parents):
    return None
