DESCRIPTION = """Check if Grid can be Cut into Sections

You are given an integer n representing the dimensions of an n x n grid, and
a 2D array rectangles, where rectangles[i] = [startx_i, starty_i, endx_i, endy_i]
represents the ith rectangle.

Return true if you can make either two horizontal or two vertical cuts on the
grid such that each of the three resulting sections contains at least one rectangle.
Every rectangle belongs to exactly one section.

Example:
    Input: n = 5, rectangles = [[1,0,5,2],[0,2,2,4],[3,2,5,3],[0,4,4,5]]
    Output: True

"""


def solution(n, rectangles):
    return None
