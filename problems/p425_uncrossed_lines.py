DESCRIPTION = """Uncrossed Lines

You are given two integer arrays `nums1` and `nums2`. We write the integers of nums1
and nums2 (in the order given) on two separate horizontal lines.

We may draw connecting lines: a straight line connecting nums1[i] and nums2[j] such
that nums1[i] == nums2[j], and the line we draw does not intersect any other connecting
(non-horizontal) line.

Return the maximum number of connecting lines we can draw in this way.

Example:
    Input: nums1 = [1,4,2], nums2 = [1,2,4]
    Output: 2
    Explanation: Draw line from nums1[0]=1 to nums2[0]=1 and nums1[1]=4 to nums2[2]=4.
"""


def solution(nums1, nums2):
    return None
