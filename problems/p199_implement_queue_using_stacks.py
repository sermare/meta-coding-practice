DESCRIPTION = """Implement Queue using Stacks

Implement a first in first out (FIFO) queue using only two stacks. The queue
should support: push (to back), peek (front element), pop (remove front), and
empty.

For this problem, solution(operations, args) simulates the calls and returns
results.

Example:
    Input: operations = ["push","push","peek","pop","empty"]
           args = [[1],[2],[],[],[]]
    Output: [None, None, 1, 1, False]
"""


def solution(operations, args):
    return None
