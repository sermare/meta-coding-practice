DESCRIPTION = """Serialize and Deserialize BST

Design an algorithm to serialize and deserialize a binary search tree.
Serialization is converting a data structure into a sequence of bits so that
it can be stored or transmitted. Deserialization reconstructs the tree from
the serialized string.

The encoded string should be as compact as possible.

Example:
    Input: root = [2,1,3]
    Output: [2,1,3]

Example:
    Input: root = []
    Output: []
"""


def solution(root):
    return None
