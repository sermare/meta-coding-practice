DESCRIPTION = """Word Ladder

A transformation sequence from word beginWord to word endWord using a
dictionary wordList is a sequence such that the first word is beginWord,
the last word is endWord, and each adjacent pair of words differs by a
single letter. Every word in the sequence must be in wordList (beginWord
does not need to be).

Given two words, beginWord and endWord, and a dictionary wordList, return
the number of words in the shortest transformation sequence, or 0 if no
such sequence exists.

Example:
    Input: beginWord = "hit", endWord = "cog",
           wordList = ["hot","dot","dog","lot","log","cog"]
    Output: 5
    Explanation: hit -> hot -> dot -> dog -> cog

Example:
    Input: beginWord = "hit", endWord = "cog",
           wordList = ["hot","dot","dog","lot","log"]
    Output: 0
"""


def solution(beginWord, endWord, wordList):
    return None
