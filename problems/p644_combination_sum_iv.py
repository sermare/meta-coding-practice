DESCRIPTION = """Combination Sum IV

Given an array of distinct integers `nums` and a target integer `target`,
return the number of possible combinations that add up to target.

The order of the numbers in the combination matters (different sequences
are counted as different combinations).

Example:
    Input: nums = [1,2,3], target = 4
    Output: 7
    Explanation: The possible combinations are:
                 (1,1,1,1), (1,1,2), (1,2,1), (1,3),
                 (2,1,1), (2,2), (3,1)

Example:
    Input: nums = [9], target = 3
    Output: 0
"""


def solution(nums, target):
    return None
