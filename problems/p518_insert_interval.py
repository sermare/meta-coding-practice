DESCRIPTION = """Insert Interval

You are given an array of non-overlapping intervals sorted by start time and a new
interval. Insert the new interval into the intervals (merge if necessary) and return
the result, keeping intervals sorted and non-overlapping.

Example:
    Input: intervals = [[1,3],[6,9]], newInterval = [2,5]
    Output: [[1,5],[6,9]]
"""


def solution(intervals, newInterval):
    return None
