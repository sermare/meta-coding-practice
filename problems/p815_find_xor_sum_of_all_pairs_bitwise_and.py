DESCRIPTION = """Find XOR Sum of All Pairs Bitwise AND

The XOR sum of a list is the bitwise XOR of all its elements. If the list only
contains one element, then its XOR sum will be equal to this element.

Given two 0-indexed integer arrays arr1 and arr2, return the XOR sum of the list
where each element equals arr1[i] AND arr2[j] for every 0 <= i < len(arr1)
and 0 <= j < len(arr2).

Example:
    Input: arr1 = [1,2,3], arr2 = [6,5]
    Output: 0

"""


def solution(arr1, arr2):
    return None
