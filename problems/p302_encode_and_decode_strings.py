DESCRIPTION = """Encode and Decode Strings

Design an algorithm to encode a list of strings to a single string. The encoded
string is then decoded back to the original list of strings.

Implement the `encode` and `decode` functions. The solution function should return
a tuple (encode, decode) of callables.

Example:
    Input: strs = ["hello", "world"]
    Encoded: "5#hello5#world"
    Output: ["hello", "world"]
"""


def solution(strs):
    return None
