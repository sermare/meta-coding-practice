DESCRIPTION = """Goat Latin

You are given a string `sentence` of words separated by spaces. Convert to Goat Latin:
- If a word begins with a vowel (a, e, i, o, u), append "ma" to the end.
- If a word begins with a consonant, remove the first letter, append it to the end,
  then add "ma".
- Add one letter 'a' to the end per word index (1-indexed).

Return the final sentence.

Example:
    Input: sentence = "I speak Goat Latin"
    Output: "Imaa speakmaaa Goatmaaaa Latinmaaaaa"
"""


def solution(sentence):
    return None
