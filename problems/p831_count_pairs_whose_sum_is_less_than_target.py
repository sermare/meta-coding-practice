DESCRIPTION = """Count Pairs Whose Sum is Less than Target

Given a 0-indexed integer array nums of length n and an integer target, return
the number of pairs (i, j) where 0 <= i < j < n and nums[i] + nums[j] < target.

Example:
    Input: nums = [-1, 1, 2, 3, 1], target = 2
    Output: 3
    Explanation: Pairs: (-1,1), (-1,1), (-1,1) -> indices (0,1),(0,4),(0,2) wait
                 Actually (-1)+1=0<2, (-1)+2=1<2, (-1)+1=0<2 -> 3 pairs.

"""


def solution(nums, target):
    return None
