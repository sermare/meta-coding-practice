DESCRIPTION = """Fruit Into Baskets

You are visiting a farm with a row of fruit trees represented by an integer array
`fruits` where fruits[i] is the type of fruit the ith tree produces.

You have two baskets, and each basket can only hold a single type of fruit. There is
no limit on the amount of fruit each basket can hold.

Starting from any tree, you must pick exactly one fruit from every tree while moving
to the right. You must stop when you encounter a third fruit type.

Return the maximum number of fruits you can pick.

Example:
    Input: fruits = [1, 2, 1]
    Output: 3

Example:
    Input: fruits = [0, 1, 2, 2]
    Output: 3
"""


def solution(fruits):
    return None
