DESCRIPTION = """Swim in Rising Water

You are given an n x n integer matrix `grid` where each value grid[i][j] represents
the elevation at that point.

When it rains, at time t, the depth of water everywhere is t. You can swim from a cell
to another 4-directionally adjacent cell if the elevation of both cells is at most t.

Return the least time until you can reach from the top left (0,0) to the bottom right
(n-1, n-1).

Example:
    Input: grid = [[0,2],[1,3]]
    Output: 3
"""


def solution(grid):
    return None
