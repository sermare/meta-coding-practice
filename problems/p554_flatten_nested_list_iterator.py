DESCRIPTION = """Flatten Nested List Iterator

You are given a nested list of integers. Each element is either an integer or a list
whose elements may also be integers or other lists. Implement an iterator to flatten it.

Implement the NestedIterator class:
- NestedIterator(nestedList) initializes the iterator with the nested list.
- next() returns the next integer in the nested list.
- hasNext() returns True if there are still some integers in the nested list.

For this problem, return the flattened list of integers given a nested structure
represented as nested Python lists and ints.

Example:
    Input: nestedList = [[1,1],2,[1,1]]
    Output: [1,1,2,1,1]

    Input: nestedList = [1,[4,[6]]]
    Output: [1,4,6]
"""


def solution(nestedList):
    return None
