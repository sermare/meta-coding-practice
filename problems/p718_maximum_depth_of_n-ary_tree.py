DESCRIPTION = """Maximum Depth of N-ary Tree

Given an N-ary tree, find its maximum depth.

The maximum depth is the number of nodes along the longest path from the root node
down to the farthest leaf node.

The tree is given as a nested list where each element is [val, [children...]].

Example:
    Input: root = [1, [[3, [[5, []], [6, []]]], [2, []], [4, []]]]
    Output: 3
"""


def solution(root):
    return None
